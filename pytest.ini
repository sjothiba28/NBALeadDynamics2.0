[pytest]
testpaths = tests
markers =
    network: hits stats.nba.com; deselected by default
# The offline suite is the default so a flaky or unreachable API never fails a
# run. Use `-m network` for the live baseline checks, `-m ''` for everything.
addopts = -m "not network" --strict-markers
